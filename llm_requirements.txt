# LLM Integration Dependencies for politics.py

# Core dependencies (always required)
requests>=2.28.0
transformers>=4.20.0
torch>=1.12.0

# Optional LLM providers (install as needed)
openai>=1.0.0          # For OpenAI GPT models
anthropic>=0.3.0       # For Claude models  
huggingface-hub>=0.15.0 # For HuggingFace models

# Local LLM support
ollama                 # For local Ollama models (optional)

# Additional utilities
numpy>=1.21.0
pandas>=1.4.0

# Installation commands:
# pip install openai anthropic transformers torch requests
# pip install huggingface-hub
# 
# For local models:
# pip install ollama
# OR
# curl -fsSL https://ollama.ai/install.sh | sh
